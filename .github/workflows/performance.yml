name: My First Workflow

on:
  workflow_dispatch:
  push:

jobs:
  hello:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello Hari! My first GitHub Action."
